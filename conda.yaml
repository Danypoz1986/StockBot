channels:
  - conda-forge
dependencies:
  - python=3.12  # You can adjust the Python version if needed
  - pip
  - pip:
      - rpaframework
      - yfinance
      - requests
      - textblob
      - robocorp
